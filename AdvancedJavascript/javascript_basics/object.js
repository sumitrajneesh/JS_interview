/**
 * powerfull data structure
 */

//  var object ={
//      name:'sumit',
//      age:21,
//      hobby:'soccer',
//      isMarried:false,
//      spells:['abrakadra','shazma','boo'],
//      shour: function(){
//          console.log('Ahhhhh')
//      }
//  }
//  console.log(user.spells[1])
/**
 * console have so many function , check in chrome
 * put console
 */

 //var list = ['sumit','anish','sona']
//  var list = [
//      {
//          username:'andy',
//          password:'sec'

//  },{
//      username:'sumit',
//      password:'123',
//  }
// ]

// console.log(list[0].password)

/**
 * var emptyObje = {}
 * var nullObje = null
 * nullObje.name = "sumit" //it shows error ,we cant put value
 */

 /**
  * create facebook
  */
/*
  var database =[
      {
          username:"sumit",
          password:"supersecret"
      }
  ]

  var newsFeed = [
      {
          username:'amit',
          timelind:'so tired from all the learning'
      },
      {
          username:'Sally',
          timeline:"javascript is sooo cool"
      }
  ]


  var userNamePromt = prompt('what is your username ?')
  var passwordPromt = prompt('what is your password ?')


  function isUserValid(username, password){

    for(var i = 0; i< database.length; i++){
        if(database[i].username === username && database[i].password === password)
        {
            return true;
        }
        return false;
    }

    function signIn(username, password){
        if(isUserValid(username, password)){
            console.log(newsFeed)
        }else{
            alert('Sorry, wrong username and password')
        }
    }
    //   if(user === database[0].username && pass === database[0].password){
    //       console.log(newsFeed)
    //   }else {
    //     alert('Sorry, wrong username and password')
    //   }
  

//   signIn(userNamePromt,passwordPromt)

  /**
   * function declaration
   * function newFunction(){
   * }
   * 
   * //function expressiong
   * var newFunction = function(){
   * }
   * 
   * //calling and invoking a function
   * alert()
   * newFunction(param1,param2)
   * 
   * //assign a variable
   * var a = true;
   * //function vs method
   * function thisIsFunction(){
   * }
   * 
   * 
   * var obj = {
   * thisIsMethod: function(){
   * }
   * }
   * 
   * thisIsFunction()
   * obj.thisIsMethod()
   * 
   * 
   */

   /**
    * loops
    */

    // var todos =[
    //     "sumit",
    //     'amit',
    //     'anish',
    //     'ajay',
    //     'vijay'
    // ]
    // var todosImport =[
    //     "apple",
    //     'mango',
    //     'banga',
    //     'grapes',
        
    // ]

    // var todosLength = todos.length;

    // for(var i = 0; i< todosLength ; i++){
    //     todos.pop()
    // }

    // var counterOne = 10;
    // while(counterOne > 0){
    //     console.log(counterOne);
    //     counterOne++
    // }

    // var counterTwo = 10
    // do{
    //     counterTwo++;
    //     console.log(counterTwo);
    // }while(counterTwo > 10);

    /**
     * forEach() // difference b/w for loop and for each
     */

    //  todos.forEach(function(todo,i){
    //      console.log(todo,i)

    //  })

    // function logTodos(todo, i){ //now i can use this funciton another place
    //     console.log(todo,i);
    // }

    // todos.forEach(logTodos)
    // todosImport.forEach(logTodos)

/**
 * javascript keywords
 * break
 * case 
 * catch
 * class
 * cosnt 
 * continue
 * debugger
 * default
 * delete
 * do 
 * else export
 * extends
 * finally 
 * for 
 * function
 * if import
 * instanceof
 * new
 *  return 
 * super
 * switch
 * this
 * throw
 * try
 * typeof
 * var 
 * void 
 * while 
 * with 
 * yield
 * 
 */